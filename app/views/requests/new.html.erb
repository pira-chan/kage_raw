<%= form_for @request,:url => { action: "create"} do |f| %> 
    <%= f.file_field :image %>
    <%= f.submit %>
<% end %> 

<% @requests.each do |r| %>
  <% if r.image.attached? %>
    <%= image_tag r.image %>
  <% end %>
<% end %>

