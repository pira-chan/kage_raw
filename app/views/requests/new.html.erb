<%= form_for @request,:url => { action: "create"} do |f| %> 
    <%= f.file_field :image %>
    <%= f.submit %>
<% end %> 

<% @requests.each do |requests| %>
  <%= image_tag requests.image %>
<% end %>