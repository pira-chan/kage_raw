<%= form_for @item, url: "tag_addition_create" do |f| %>
<% #= form_with model: @item do |f| %>
        <%= fields_for :tags do |ff| %>
        <%= ff.text_field :adopt_tag, value: "動物" %>
        <%= ff.submit %>
        <% end %>
        
<% end %>

<%= form_for @item, url: "tag_addition_create" do |f| %>
        <%= fields_for :tags do |ff| %>
        <%= ff.text_field :adopt_tag, value: "カップル" %>
        <%= ff.submit %>
        <% end %>
<% end %>

<%= form_for @item, url: "tag_addition_create" do |f| %>
        <%= fields_for :tags do |ff| %>
        <%= ff.text_field :adopt_tag %>
        <%= ff.submit %>
        <% end %>
<% end %>