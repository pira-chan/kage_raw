<%= form_for @item, url: "tag_addition" do |f| %>
        <%= fields_for :tags do |f| %>
        <%= f.text_field :adopt_tag, value: "人間" %>
        <% end %>
        <%= f.submit%>
<% end %>

<%= form_for @item, url: "tag_addition" do |f| %>
        <%= fields_for :tags do |f| %>
        <%= f.text_field :adopt_tag, value: "男性" %>
        <% end %>
        <%= f.submit%>
<% end %>